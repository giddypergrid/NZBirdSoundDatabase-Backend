# ─────────────────────────────────────────────────────────────
# Single source of truth for ALL Python dependencies in this repo.
# Covers: DjangoProject, MaterialsPrep (+ embedding pipeline),
# BirdClassify, BirdSoundTraining, birdTextTraining.
#
# Used by:
#   - Local venv:  pip install -r DjangoProject/requirements.txt
#   - Docker:      Dockerfile copies this file in.
# ─────────────────────────────────────────────────────────────

# ── Django web stack ─────────────────────────────────────────
Django==5.2.10
djangorestframework==3.15.2
drf-spectacular==0.27.2
django-cors-headers==4.4.0
django-prometheus==2.3.1
psycopg[binary]==3.2.3
gunicorn==23.0.0
whitenoise==6.8.2
python-dotenv==1.0.1
psutil>=5.9,<7.2          # autogluon-common pins psutil<7.2

# ── Observability ────────────────────────────────────────────
sentry-sdk[django]==2.18.0
python-json-logger==2.0.7

# ── Bird audio + ML core (classifier, embeddings) ────────────
Pillow==10.4.0
numpy<2.4
pandas<2.4
scikit-learn
birdnetlib==0.18.0
librosa>=0.9.0
resampy==0.4.2
tensorflow>=2.8.0
sentence-transformers

# ── Tabular classifier (BirdClassify) ────────────────────────
typing_extensions
autogluon.tabular[lightgbm]==1.5.0
xgboost-cpu==3.2.0
